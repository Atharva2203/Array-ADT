#include <iostream>
using namespace std;

float Avg(int arr[],int n)
{
    int total=0;
    for(int i=0;i<n;i++)
    {
       total+=arr[i];
    }
    return float(total)/n;
}

int main()
{
    int arr[]={20,3,14,28,4};
    int n=sizeof(arr)/sizeof(int);
    cout<<Avg(arr,n);
    return 0;
}
