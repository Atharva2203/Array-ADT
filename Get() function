#include <iostream>
using namespace std;

int Get(int arr[],int index,int n)
{
    if(index>=0 && index<n)
        return arr[index];
    return -1;    
}


int main()
{
    int index;
    int arr[]={2,3,15,20,40};
    int n=sizeof(arr)/sizeof(int);
    cout<<Get(arr,4,n);
    return 0;
}
